apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: demo
spec:
  # LoadBalancer provisions an AWS ELB automatically — one public endpoint
  # for the whole demo. nginx proxies /api internally to api-server.
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
