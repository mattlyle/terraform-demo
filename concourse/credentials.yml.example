# Non-sensitive pipeline configuration for set-pipelines.sh.
# AWS credentials are NOT stored here -- they are read from your shell environment.

git_repo_uri: "https://github.com/your-username/terraform-demo.git"
git_branch: "main"
aws_region: "us-east-1"
eks_cluster_name: "matt-lyle-terraform-demo-eks"
